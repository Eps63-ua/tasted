import { router } from 'expo-router';import { MailCheck } from 'lucide-react-native';import { AppText,Button,Screen } from '@/components/ui/app-ui';import { colors } from '@/constants/theme';export default function Page(){return <Screen><MailCheck size={48} color={colors.accent}/><AppText variant="screen">Confirma tu correo</AppText><AppText color={colors.textSecondary}>Hemos enviado un enlace a tu bandeja de entrada. Después de confirmarlo, vuelve para iniciar sesión.</AppText><Button title="Ir a iniciar sesión" onPress={()=>router.replace('/login')}/></Screen>}
