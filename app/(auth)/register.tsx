import { AuthForm } from '@/components/forms/auth-form';import { AppHeader,AppText,Screen } from '@/components/ui/app-ui';import { colors } from '@/constants/theme';export default function Page(){return <Screen><AppHeader title="Crear cuenta" back/><AppText color={colors.textSecondary}>Tu catálogo será compartido; tu diario y categorías seguirán siendo privados.</AppText><AuthForm mode="register"/></Screen>}
