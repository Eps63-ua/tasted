import { useLocalSearchParams } from 'expo-router';import { CategoryForm } from '@/components/forms/entity-forms';import { AppHeader, EmptyState, Screen } from '@/components/ui/app-ui';import { useAppData } from '@/providers/app-data-provider';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();const {data}=useAppData();const item=data?.categories.find(x=>x.id===id);return <Screen><AppHeader title="Editar categoría" back/>{item?<CategoryForm category={item}/>:<EmptyState title="No encontrada" message="Esta categoría no existe."/>}</Screen>}
