import { useLocalSearchParams } from 'expo-router';import { EstablishmentForm } from '@/components/forms/entity-forms';import { AppHeader, EmptyState, Screen } from '@/components/ui/app-ui';import { useAppData } from '@/providers/app-data-provider';export default function Page(){const {id}=useLocalSearchParams<{id:string}>();const {data}=useAppData();const item=data?.establishments.find(x=>x.id===id);return <Screen><AppHeader title="Editar establecimiento" back/>{item?<EstablishmentForm establishment={item}/>:<EmptyState title="No encontrado" message="Este establecimiento no existe."/>}</Screen>}
